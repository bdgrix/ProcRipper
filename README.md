# ProcRipper
Game-focused thread optimizer that applies custom priority, affinity, and boost settings based on config files. Detects game processes, initializes thread rules automatically, and separates game/non-game configs
